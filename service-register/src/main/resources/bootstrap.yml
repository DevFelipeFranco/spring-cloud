server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    registerWithEureka: false

spring:
  application:
    name: service-register
  cloud:
    config:
      uri: http://localhost:8888
      username: felipe
      password: franco