server:
  port: 8888

spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DevFelipeFranco/microservice-config-server.git
  security:
    user:
      name: felipe
      password: franco
#spring:
#  application:
#    name: service-config
#  cloud:
#    config:
#    server:
#    git:
#    uri: file:///E:/Poli2021-1/CLOUD/Repos/config-data
#  security:
#    user:
#    name: ${CONFIG_USER}
#    password: ${CONFIG_PASSWORD}
